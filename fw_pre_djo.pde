import java.util.Arrays;

int backgroundColor = 0;

Engine engine;

void setup() {
    size(800, 600);

    engine = new Engine();
}

void draw() {
    background(backgroundColor);

    // === MenuScreen
    // engine.update();
    // engine.draw();

    // engine.showStatistics();
}

